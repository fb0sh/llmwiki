# LLM Wiki — Schema

This document defines how I maintain this wiki. Read it on every session start. It is the single source of truth for conventions, workflows, and structure.

## 核心原则

1. **Wiki 是持久化的复合产物** — 知识被编译一次，持续更新，而不是每次查询重新推导。
2. **LLM 写，人类读** — 我（LLM）负责所有书摘、总结、交叉引用、文件管理。人类负责策展源材料、提问、指导方向。
3. **原始文档不可变** — `raw/` 下的文件只读不改。所有派生知识写入 `wiki/`。
4. **写入需要授权** — 默认只读。只有两种情况可以写 wiki：人类明确要求记住/归档/更新页面，或本次调用了 skill（skill 内部定义的写入属于授权范围）。其余情况只在对话中回答，不落盘。知识在 wiki 中积累，但积累由人类批准触发，我不擅自归档。

## 记忆边界

**默认只读。** 未经授权不写入任何文件 —— 包括 `wiki/` 页面、`index.md`、`log.md`、`raw/.ingest-state.json`。

允许写入的两种情况：

| 触发 | 可写范围 |
|------|----------|
| 人类明确要求：「记住」「记入 wiki」「归档」「更新某页」 | 人类所指的那部分内容 |
| 人类调用了 skill | 该 skill 定义的写入步骤 |

禁止：

- 把对话中的临时上下文、猜测、随口结论写成 wiki 页面
- 因为"可能有价值"就主动归档到 `wiki/qa/`
- 未经要求就顺手更新交叉引用、`links` 计数或追加 `log.md`
- 把人类的个人信息、偏好、私人对话记入仓库

不确定是否该写入时，先问一句，不要先写再报告。

## 目录结构

```
llmwiki/
├── AGENTS.md                # 本文件 — 行为规范
├── README.md                # 人类入口 — 是什么 + 怎么用
├── index.md                 # 内容目录 — 每个页面的链接 + 一行摘要
├── log.md                   # 操作日志 — 追加记录，按日期分区
├── .skills/                 # llmwiki 专用 skill（版本控制）
│   ├── llmwiki-ingest/      #   摄取工作流
│   ├── llmwiki-query/       #   查询工作流
│   ├── llmwiki-doctor/      #   健康检查工作流
│   ├── llmwiki-sync/        #   仓库同步工作流
│   ├── llmwiki-export/      #   知识备份（打包知识层）
│   ├── llmwiki-restore/     #   知识恢复（换机器补回 raw / 回滚快照）
│   ├── llmwiki-update/      #   框架更新（从上游模板覆盖框架）
│   ├── llmwiki-gen-web/     #   静态网站生成
│   └── llmwiki-image-ocr/   #   图片 OCR 提取
├── raw/                     # 原始文档（不可变）
│   ├── assets/              # 图片、附件（从网页裁剪下载）
│   ├── .ingest-state.json   # SHA256 状态（增量检测，单行 JSON）
│   └── *.md                 # 源文档：文章、论文、笔记
├── wiki/                    # 编译知识
│   ├── _index_.md           # 欢迎页 / 顶层概览
│   ├── concepts/            # 概念页 — 独立的概念、模式、原理
│   ├── entities/            # 实体页 — 人物、地点、作品、事物
│   ├── sources/             # 源摘要 — 每个 raw 源对应一页摘要
│   └── qa/                  # 归档查询 — 有价值的问答、比较、分析
├── .agents/                 # 编码 agent 的项目级发现入口（软链 → .skills/）
└── scripts/                 # 辅助工具

> `.agents/skills/`（软链到 `.skills/`）是编码 agent 的项目级技能发现入口，不绑定具体某款 agent。技能源码在 `.skills/` 下版本控制。

## 页面规范

### 通用规则

- **格式**: GitHub Flavored Markdown + 可选 YAML frontmatter
- **交叉引用**: 使用相对链接 `[概念名](../wiki/concepts/概念名.md)` 或 `[源摘要](../wiki/sources/源摘要.md)`
- **引用源头**: 提及从源文档提取的信息时，在括号中标注 `(src: raw/文件名.md)`
- **矛盾标注**: 当不同源对同一事实有不同说法时，明确标注 `⚠️ 矛盾` 并列出双方观点及来源
- **过时标注**: 当新源覆盖或修正旧信息时，在旧页面顶部添加 `> [!NOTE] 此页信息已被 [新页面](../wiki/...) 更新/修正`

### YAML Frontmatter（推荐）

```yaml
---
title: 页面标题
tags: [标签1, 标签2]
created: 2026-07-14
updated: 2026-07-14
sources: [raw/源文件1.md, raw/源文件2.md]
links: 42  # 此页面被其他页面引用的次数（lint 时更新）
---
```

### 概念页 (`wiki/concepts/`)

每个重要的独立概念一页。结构：
- 一句话定义
- 详细解释
- 关键属性/特征
- 与其他概念的链接
- 来源引用

### 实体页 (`wiki/entities/`)

人物、作品、工具、组织等。结构：
- 基本标识（名称、类型）
- 关键事实清单
- 关系网络（链接到其他实体/概念）
- 来源记录

### 源摘要 (`wiki/sources/`)

每个 raw 源对应一页摘要。结构：
- 元数据：标题、作者、日期、URL
- 核心论点/内容摘要（3-5 段）
- 关键引文
- 提取的实体和概念列表（自动链接到 wiki 中已有页面）
- 个人思考/评论

### QA 归档 (`wiki/qa/`)

有价值的查询回答。结构：
- 问题
- 回答摘要（含 wiki 内引用）
- 如果产生新的洞见，同时更新相关概念/实体页

## 默认处理流程

当用户提出问题时，按以下步骤处理：

1. **识别意图**
   - 是知识查询？（查 wiki 已有内容、比较、分析）
   - 是内容摄取？（新源文档放入 `raw/` → 编译到 `wiki/`）
   - 是内容整理？（改写、重组、合并现有页面）
   - 是健康检查？（lint wiki：矛盾、过时、孤立页面、缺失交叉引用）
   - 是维护操作？（更新 index、修复链接、清理过时内容）

2. **定位资料**
   - 优先读 `index.md` 快速定位相关页面
   - 再读 `wiki/` 下对应的概念页、实体页、源摘要
   - 必要时回溯 `raw/` 源文档核验原文
   - 查看 `log.md` 了解最近操作

3. **筛选信息**
   - 只保留与当前问题直接相关的内容
   - 去掉重复、过时、已被新源覆盖的信息
   - 不编造 wiki 中不存在的事实；不确定时标注 `[待核实]`

4. **综合输出**
   - 根据目标选择合适结构
   - 引用 wiki 内页面时使用相对链接
   - 标注信息来源（`src: raw/文件名.md`）

5. **归档与收尾**（仅在获得写入授权时执行，见「记忆边界」）
   - 人类要求归档，且回答产生了新洞见（比较、分析、新连接）→ 写入 `wiki/qa/`
   - 人类要求更新某页 → 同步更新对应概念页或实体页
   - 有页面增删 → 更新 `index.md`
   - 每次实际写入后 → 在 `log.md` 追加操作记录

6. **给出下一步**
   - 如果资料缺失，说明缺少什么、建议补充到哪里
   - 如果回答产生了值得沉淀的洞见，**建议**归档到 `wiki/qa/`，由人类决定
   - 如果可以继续细化，列出可选方向

## 信息使用优先级

1. 当前对话中的明确要求
2. 本文件 `AGENTS.md`
3. `index.md`（内容目录，定位入口）
4. `wiki/` 下对应页面（概念、实体、源摘要、QA）
5. `raw/` 源文档原文
6. `log.md`（操作历史）
7. 必要时再使用通用推理

## 内容放置规则

新内容到达时，按以下规则判断落点。如果一条内容同时匹配多条，以先出现的为准。

### 原始文档 → `raw/`

- 来自外部的文章、论文、网页存档、笔记原文
- **只读不修改** — 这是源材料层
- 放入后更新 `raw/.ingest-state.json`（追加 SHA256）
- 附件入 `raw/assets/`

### 源摘要 → `wiki/sources/`

- 每个 `raw/` 下的源文档对应一页摘要
- 创建或更新摘要后，必须同步更新相关概念页和实体页

### 概念页 → `wiki/concepts/`

- 独立的概念、模式、原理、方法论
- 判断标准：能否用一句话定义？是否被多个源反复提及？
- 只在单一源中简短出现的概念，不必单独建页，在源摘要中覆盖即可

### 实体页 → `wiki/entities/`

- 人物、组织、作品、工具、事件
- 信息量不足以成页的实体，合并到引用的概念页或源摘要中

### QA 归档 → `wiki/qa/`

- 有价值的问答、比较、分析
- 判断标准：这个问题是否可能再次被问到？回答是否产生了新连接？
- **仅在获得写入授权时落盘**（人类要求归档，或走了 skill 流程）
- 同一问题有新答案时在原文件追加 `## 更新 (YYYY-MM-DD)`，不新建

### 索引与日志

- **`index.md`**: 每个新建/删除的页面同步更新。每行格式：`- [标题](路径) — 一句话摘要`
- **`wiki/_index_.md`**: 顶层概览，wiki 结构显著变化时更新
- **`log.md`**: 每次操作后追加一行 `## [YYYY-MM-DD] 操作类型 | 描述`，不改写历史

### 草稿

- 直接写入目标位置，在正文顶部加 `> 🚧 草稿` 标记
- 完成后去掉标记
- 不设独立的临时目录

### 不应放入本仓库

- 二进制可执行文件、数据库文件
- 有版权限制不允许再分发的全文（用链接替代）
- 个人敏感信息（密钥、密码、私人对话）
- 与 wiki 主题无关的个人笔记

## 澄清规则

只有在以下情况才追问用户：

1. 缺少关键信息，无法判断是查询、摄取还是维护操作
2. 同一源文件存在多种合理处理方式，且结果差异很大
3. 需要用户提供缺失的源材料、元数据或具体方向

如果可以合理假设，先给出可用版本，并在输出中标注假设。

## 默认输出风格

- 默认使用中文（源材料为英文且术语保留英文的情况除外）
- 结构清晰，优先使用标题、列表和步骤
- 中英文之间加空格（"这是一个 LLM 驱动的 wiki"）
- 术语首现时标注英文原文
- 代码块、文件名、路径用反引号
- 引用 wiki 页面时使用相对链接
- 不堆砌无关解释，直接给出可用结果
- 如果是整理知识，输出应便于之后复用
- 如果只是文件操作，简要说明改动即可

## 质量标准

每次输出应满足：

- **准确**: 不编造事实，所有声明可追溯到 `raw/` 源文档或 `wiki/` 已有页面
- **相关**: 只保留对当前问题有帮助的信息，去掉噪音
- **可用**: 结果能直接使用、保存或继续迭代
- **可维护**: 结构方便未来扩展和交叉引用
- **可追溯**: 标注信息来自哪个源或页面（`src:` 标注或相对链接）

---

## 工作流

### 1. 摄取 (Ingest)

当人类把源放入 `raw/`，或直接给出一个 `raw/` 之外的路径 / 链接 / 一段文字，并说"处理这个"：

0. 源不在 `raw/` 时，自己转换并放进 `raw/`（anydoc、HTML 用 pandoc、图片/扫描件走 `llmwiki-image-ocr`，或直接 `./scripts/ingest.sh <文件>`）—— 不要让人类先转好或手动搬进来
1. 读取原始文档
2. 与人类讨论关键要点（可选）
3. 在 `wiki/sources/` 写摘要页
4. 更新或创建相关 `wiki/concepts/` 和 `wiki/entities/` 页面
5. 更新 `index.md`（新页面 + 更新现有条目）
6. 在 `log.md` 追加条目：`## [YYYY-MM-DD] ingest | 文章标题`
7. 如果 `wiki/_index_.md` 需要更新顶层概述，一并更新

一个源可能涉及 5-15 个 wiki 页面。每条更新都要做交叉引用维护。

### 2. 查询 (Query)

当人类提问时：

1. 先读 `index.md` 快速定位相关页面
2. 读相关页面内容
3. 综合回答，引用 wiki 内页面
4. 只在人类要求归档时，才把回答写入 `wiki/qa/`；否则在结尾建议归档，由人类决定

### 3. 检查 (Lint)

当人类说"健康检查"或"lint"：

1. 扫描 wiki 查找：
   - 页面间的矛盾
   - 被新源取代的过时声明
   - 零入链的孤立页面
   - 经常被提及但没有独立页面的概念
   - 缺失的交叉引用
2. 建议可调查的新问题和新源
3. 更新 `index.md` 中的 `links` 计数
4. 在 `log.md` 追加 lint 条目

### 4. 备份与恢复 (Backup / Restore)

当人类说"备份 / 导出"，或给了归档地址说"恢复 / 导入"：

- **备份** → `llmwiki-export`：`./scripts/export.sh` 打包 `index.md` + `log.md` + `wiki/` + `raw/` 到 `backups/*.tar.gz`。默认含 `raw/`，因为 `raw/` 被 `.gitignore` 排除、git 里没有源材料。**框架文件不进归档。**
- **恢复** → `llmwiki-restore`：`./scripts/restore.sh <归档或URL> --target <目录>`，只写知识层，框架文件保持目标仓库原样。用途是**换机器补回 `raw/` 源材料、回滚知识快照、迁到新仓库** —— 框架升级不走这里（那是 `update.sh`）。
- 恢复脚本有护栏（归档身份 / SHA256 / 覆盖保护），**不要绕过**；`--force` 是破坏性操作，执行前必须让人类确认。
- 备份完必须提醒人类把归档拷到仓库之外 —— 留在 `backups/` 不算备份。

### 5. 框架更新 (Update)

当人类说"更新框架 / 升级模板 / 拉上游"：

- 上游是模板仓库 `https://github.com/fb0sh/llmwiki.git`（或人类自己的 fork）
- **不要用 `git merge upstream/main`** —— Use this template 不复制历史，两边没有共同祖先，必然 `refusing to merge unrelated histories`
- 正确做法 → `llmwiki-update`：`./scripts/update.sh`，取上游文件树只替换框架路径（`.skills/`、`.agents/`、`scripts/`、`AGENTS.md`、`README.md`、`package.json`、`package-lock.json`、`mise.toml`、`.gitignore`），`index.md` / `log.md` / `wiki/` / `raw/` 一律不碰
- 先 `--dry-run`；本地自建的 skill 默认保留，`--prune` 才删；框架文件有未提交改动时会拒绝，需先提交或 `--force`
- 仓库里还没有 `scripts/update.sh` 时，用上游副本引导：`git clone --depth 1 <上游> /tmp/tpl && /tmp/tpl/scripts/update.sh --target "$PWD"`

## 工具

- **anydoc** (Firecrawl): 将 Word/PPT/Excel/PDF/EPUB/CSV 等转为 GFM Markdown 用于摄取（格式从字节识别，HTML 和图片不支持）
- **index.md**: 主目录，查询时先读它
- **log.md**: 时间线，了解最近操作
- **sha256sum**: 计算文件哈希，用于增量检测
- **raw/.ingest-state.json**: 已处理文件的 SHA256 哈希集合（纯数组，无元数据）
- **scripts/export.sh** / **scripts/restore.sh**: 知识备份与恢复（见 `.skills/llmwiki-export/`、`.skills/llmwiki-restore/`）
- **scripts/update.sh**: 框架更新 —— 从上游模板覆盖框架文件（见 `.skills/llmwiki-update/`）
